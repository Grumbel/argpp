#ifndef HEADER_BUILDER_HPP
#define HEADER_BUILDER_HPP

class Builder
{
private:
public:
  Builder();

private:
  Builder(const Builder&) = delete;
  Builder& operator=(const Builder&) = delete;
};

#endif

/* EOF */
